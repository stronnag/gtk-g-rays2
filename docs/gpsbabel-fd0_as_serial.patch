--- gbser_posix.c.orig  2007-05-18 18:29:13.000000000 +0100
+++ gbser_posix.c       2007-05-18 18:30:33.000000000 +0100
@@ -117,7 +117,12 @@
        h->magic = MYMAGIC;
        h->vmin = h->vtime = 0;
 
-    if (h->fd = open(port_name, O_RDWR | O_NOCTTY), h->fd == -1) {
+        if(*port_name == '-' && *(port_name+1) == 0)
+        {
+            h->fd = 0;
+            return h;
+        }
+        else if (h->fd = open(port_name, O_RDWR | O_NOCTTY), h->fd == -1) {
                gbser__db(1, "Failed to open port (%s)\n", strerror(errno));
         goto failed;
     }
